import { LangExtension } from '../types';

/**
 * Extensiones de archivo válidas
 */
export const JSON_EXTENSION: LangExtension = '.json';
export const JS_EXTENSION: LangExtension = '.js';
export const TS_EXTENSION: LangExtension = '.ts';

/**
 * Array de extensiones de archivo válidas
 */
export const VALID_FILE_EXTENSIONS: LangExtension[] = [
  JSON_EXTENSION,
  JS_EXTENSION,
  TS_EXTENSION,
]; 